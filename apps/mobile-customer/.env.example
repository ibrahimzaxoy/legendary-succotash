VITE_API_URL=http://localhost:3000
# Each deployment of this app is one restaurant's own branded app (like a
# single chain's ordering app, not a multi-restaurant marketplace) - this
# is that restaurant's id.
VITE_RESTAURANT_ID=
